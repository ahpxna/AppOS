# job-apply-os -- top-level requirements aggregator.
#
# Services with external Python dependencies keep local requirements files.
# This file aggregates the supported runtime/developer dependency set so
# a fresh clone can do one install instead of installing each service separately:
#
#   python -m venv .venv
#   source .venv/bin/activate        # Windows/WSL: same command inside WSL bash
#   pip install -r requirements.txt
#
# `services/browser-controller/requirements.txt` also documents two
# system binaries (poppler, tesseract) that pip cannot install -- see
# README.md "OCR binaries" section for the Windows/WSL install commands.

-r services/discovery/requirements.txt
-r services/browser-controller/requirements.txt
-r services/document-generation/requirements.txt
-r services/review/requirements.txt
-r services/telegram/requirements.txt
-r services/profile-ingestion/requirements.txt

# Developer/runtime tools referenced by README and the local services.
# Keep these here so a clean `pip install -r requirements.txt` can actually
# run the migration linter and regression suite documented in README.md.
pglast>=7,<9
pytest>=8.0,<9

# Needed only when the consent-based pointer recorder is used locally.
pynput>=1.7,<2

# Local credential vault (AES-256-GCM; secrets never stored plaintext in PostgreSQL).
cryptography>=45,<47
