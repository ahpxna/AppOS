# JobOS V1 reproducibility constraints.
#
# Keep requirements files semantically broad enough for service ownership, but
# install release/test environments with this constraint set so a future pip
# resolver cannot silently move JobOS onto an unverified major/minor runtime.
# Update intentionally together with the regression suite and release notes.
psycopg[binary]==3.3.4
pglast==8.4
pytest==8.4.2
pynput==1.8.2
cryptography==46.0.4
python-docx==1.2.0
pypdf==5.9.0
requests==2.32.5
websocket-client==1.9.0
reportlab==4.4.9
